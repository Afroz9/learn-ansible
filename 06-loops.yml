- name: Demo on loops
  hosts: localhost
  connection: local
  tasks:

    - name: Print Fruit name
      ansible.builtin.debug:
        msg: Fruit Name - {{ item }}
      loop:
          - apple
          - orange
          - grape
          - banana